use diesel::prelude::*;

pub struct {{capitalize_feature_name}}DataSource;

impl {{capitalize_feature_name}}DataSource {
    pub fn new() -> {{capitalize_feature_name}}DataSource {
        {{capitalize_feature_name}}DataSource {}
    }

    // pub fn sign_up(
    //     &self,
    //     new_credentials_info: NewCredentialsInfoDto,
    // ) -> Result<CredentialsInfoDto, CustomDatabaseError> {
    //     let mut connection = connection_pool::get_connection();

    //     ...
    // }
}
