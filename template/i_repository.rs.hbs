use crate::common::domain::failure::failure::IFailure;

pub trait I{{capitalize_feature_name}}Repository {
    // fn sign_up(&self, entity: CredentialsInfoEntity) -> Result<TokenEntity, Box<dyn IFailure>>;
}
